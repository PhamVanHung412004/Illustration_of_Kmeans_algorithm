# lưu ý mọi người nhớ tạo lại folder list_img rỗng không có gì trước khi chạy chương trình
# trước khi chạy chương trình thì mở terminal gõ: pip install -r requirements để cài đặt các thư viện để chạy chương trình
# sau đó chạy file KMeans.py thì sẽ chạy được chương trình minh họa thuật toán kmeans và cách tiến hành thuật toán: 
[Video demo](video/video1.mkv)
# Ta áp dụng thuật toán KMeans vào xử lý ảnh. Chạy file Img_KMeans.py hình ảnh sau khi chạy: 
[Video demo](video/video2.mkv) 

